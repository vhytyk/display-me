@model DisplayMe.Models.TryvohaPayload
<html>
<head>
    <title>Церква Святого Предікшна "Храм Тривоги"</title>
    <link rel="stylesheet" href="~/css/site.css">
    <meta http-equiv="refresh" content="10">
    <link rel="icon" href="/favicon.ico" />
</head>
<body>
    @if (Model != null)
    {
    <h3>Церква Святого Предікшна - "Храм Тривоги"</h3>
    <div>Останнє оновлення (UTC): @Model.LastUpdateTime</div><br />
    <div class="divTable">
        <div class="divTableHeading">
            <div class="divTableHead">Область</div>
            <div class="divTableHead">Стан</div>
            <div class="divTableHead">Передбачення</div>
        </div>
        @foreach (var region in Model.Regions.Keys.OrderBy(r => r))
        {
        <div class="divTableRow@(Model.Regions[region].GetRowClass())">
            <div class="divTableCell">@region</div>
            <div class="divTableCell">@(Model.Regions[region].Status ? "тривога" : "немає")</div>
            <div class="divTableCell">@(Model.Regions[region].GetPrediction())</div>
        </div>
        }
    </div>
    <br />
    <div>Про сторінку:<br/>Стан та передбачення тривог по областям, а також передбачення відбою.<br />Зроблено на основі алгоритмів і моделей машинного навчання.</div>
    <div>Дані для моделей взяті звідси https://t.me/air_alert_ua </div>
    <br />
    <div>models evaluations:</div>
    @foreach (var modelEval in Model.ModelEvaluations)
    {
    <div>@modelEval</div>
    }
    }
    else
    {
    <div>щось нема даних зовсім :(</div>
    }
</body>
</html>